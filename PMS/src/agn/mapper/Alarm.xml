<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="agn.mapper.AlarmMapper">
	<cache />

	<!-- <select id="findUser" parameterType="String" resultType="Cuser"> select 
		user_name , user_num from cuser where user_id = #{user_id} </select> <insert 
		id="insAlarm" parameterType="Alarm"> insert into alarm values (#{alarm_num}, 
		#{type_content_id, #{user_id} #{type}, #{alarm_material}, sysdate) </insert> 
		<select id="insAlarm"> select </select> -->

	<select id="todoAlarm" parameterType="int" resultType="Card">
		select c.card_content
		from card c, tag_member t, b.user_board
		where c.card_num= t.card_num
		and b.board_num = c.board_num
		and t.member_num = b.${user_num}
		and card_clear = 'n'
		order by card_due_date DESC
	</select>


</mapper>















